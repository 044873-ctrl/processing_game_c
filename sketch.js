var W=600,H=400;var paddleW=10,paddleH=50;var playerX=10,playerY=0,cpuX=0,cpuY=0;var playerSpeed=6,cpuMaxSpeed=5;var ball={x:W/2,y:H/2,r:8,vx:4,vy:3};var leftScore=0,rightScore=0;var cpuMissing=false,cpuMissTimer=0,cpuMissTargetY=0;
function resetBall(direction){ball.x=W/2;ball.y=H/2;ball.r=8;if(direction===undefined){var dir=random()<0.5?1:-1;ball.vx=4*dir;ball.vy=3*(random()<0.5?1:-1);}else{ball.vx=direction>0?4:-4;ball.vy=3*(random()<0.5?1:-1);}cpuMissing=false;cpuMissTimer=0;}
function setup(){createCanvas(W,H);playerY=H/2-paddleH/2;cpuX=W-10-paddleW;cpuY=H/2-paddleH/2;textSize(32);textAlign(CENTER,CENTER);}
function draw(){background(0);stroke(255);strokeWeight(2);for(var i=0;i<H;i+=20){line(W/2,i,W/2,i+10);}noStroke();fill(255);text(leftScore,W*0.25,30);text(rightScore,W*0.75,30);fill(255);if(keyIsDown(UP_ARROW)){playerY-=playerSpeed;}if(keyIsDown(DOWN_ARROW)){playerY+=playerSpeed;}if(playerY<0){playerY=0;}if(playerY>H-paddleH){playerY=H-paddleH;}if(cpuMissTimer>0){cpuMissTimer--;if(cpuMissTimer===0){cpuMissing=false;}}else{if(!cpuMissing&&random()<0.004){cpuMissing=true;cpuMissTimer=30;cpuMissTargetY=random(0,H-paddleH);}}var cpuTargetY=cpuMissing?cpuMissTargetY:(ball.y-paddleH/2);var dy=cpuTargetY-cpuY;if(Math.abs(dy)>cpuMaxSpeed){dy=(dy>0?1:-1)*cpuMaxSpeed;}cpuY+=dy;if(cpuY<0){cpuY=0;}if(cpuY>H-paddleH){cpuY=H-paddleH;}fill(255);rect(playerX,playerY,paddleW,paddleH);rect(cpuX,cpuY,paddleW,paddleH);ball.x+=ball.vx;ball.y+=ball.vy;if(ball.y-ball.r<=0){ball.y=ball.r;ball.vy=-ball.vy;}if(ball.y+ball.r>=H){ball.y=H-ball.r;ball.vy=-ball.vy;}if(ball.x-ball.r<=playerX+paddleW&&ball.x-ball.r>=playerX){if(ball.y>=playerY&&ball.y<=playerY+paddleH){ball.x=playerX+paddleW+ball.r;ball.vx=-ball.vx;var delta=ball.y-(playerY+paddleH/2);ball.vy=delta*0.12;}}if(ball.x+ball.r>=cpuX&&ball.x+ball.r<=cpuX+paddleW){if(ball.y>=cpuY&&ball.y<=cpuY+paddleH){ball.x=cpuX-ball.r;ball.vx=-ball.vx;var delta2=ball.y-(cpuY+paddleH/2);ball.vy=delta2*0.12;}}if(ball.x<0){rightScore++;resetBall(1);}if(ball.x>W){leftScore++;resetBall(-1);}fill(255);ellipse(ball.x,ball.y,ball.r*2,ball.r*2);}
